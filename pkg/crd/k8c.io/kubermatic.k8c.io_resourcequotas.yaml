---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
    kubermatic.k8c.io/location: master,seed
  name: resourcequotas.kubermatic.k8c.io
spec:
  group: kubermatic.k8c.io
  names:
    kind: ResourceQuota
    listKind: ResourceQuotaList
    plural: resourcequotas
    singular: resourcequota
  scope: Cluster
  versions:
    - additionalPrinterColumns:
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
        - jsonPath: .spec.subject.name
          name: Subject Name
          type: string
        - jsonPath: .spec.subject.kind
          name: Subject Kind
          type: string
      name: v1
      schema:
        openAPIV3Schema:
          description: ResourceQuota specifies the amount of cluster resources a project can use.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: Spec describes the desired state of the resource quota.
              properties:
                quota:
                  description: Quota specifies the current maximum allowed usage of resources.
                  properties:
                    accelerators:
                      description: |-
                        Accelerators holds provider-specific accelerator limits. An absent or empty list means
                        that no accelerator limits are configured. A missing provider or provider/resource pair
                        is unconstrained; this field is not an allowlist. The list is atomic so a future provider
                        scope can participate in entry identity without redefining provider as the sole map key.
                      items:
                        description: AcceleratorQuota holds accelerator limits for one KKP infrastructure provider.
                        properties:
                          provider:
                            description: |-
                              Provider is the KKP infrastructure provider identifier, not the accelerator vendor.
                              The alpha API supports only kubevirt.
                            enum:
                              - kubevirt
                            type: string
                          resources:
                            additionalProperties:
                              anyOf:
                                - type: integer
                                - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Resources contains provider-native accelerator resource names and their limits.
                              KubeVirt resource names are exact deviceName values. A missing resource name is
                              unconstrained, zero denies that resource, and a positive whole number sets its limit.
                            type: object
                        required:
                          - provider
                          - resources
                        type: object
                        x-kubernetes-validations:
                          - message: accelerator quota resources must not be empty
                            rule: self.resources.size() > 0
                      maxItems: 1
                      type: array
                      x-kubernetes-list-type: atomic
                    cpu:
                      anyOf:
                        - type: integer
                        - type: string
                      description: CPU holds the quantity of CPU. For the format, please check k8s.io/apimachinery/pkg/api/resource.Quantity.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    memory:
                      anyOf:
                        - type: integer
                        - type: string
                      description: Memory represents the quantity of RAM size. For the format, please check k8s.io/apimachinery/pkg/api/resource.Quantity.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    storage:
                      anyOf:
                        - type: integer
                        - type: string
                      description: Storage represents the disk size. For the format, please check k8s.io/apimachinery/pkg/api/resource.Quantity.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                  type: object
                subject:
                  description: Subject specifies to which entity the quota applies to.
                  properties:
                    kind:
                      default: project
                      description: Kind of the quota subject. For now the only possible kind is project.
                      enum:
                        - project
                      type: string
                    name:
                      description: Name of the quota subject.
                      type: string
                  required:
                    - kind
                    - name
                  type: object
              required:
                - quota
                - subject
              type: object
            status:
              description: Status holds the current state of the resource quota.
              properties:
                globalAcceleratorAccounting:
                  description: |-
                    GlobalAcceleratorAccounting contains the master-owned project-wide accelerator
                    accounting state. It is synchronized from the master to every Seed copy.
                  properties:
                    activationPhase:
                      description: ActivationPhase describes the current project-wide accelerator accounting state.
                      enum:
                        - Activating
                        - Ready
                        - Blocked
                      type: string
                    blockers:
                      description: Blockers contains actionable reasons why project-wide accounting is not ready.
                      items:
                        description: |-
                          AcceleratorAccountingBlocker describes an actionable reason why accelerator accounting
                          is not ready. SeedName and ClusterName identify the affected scope when applicable.
                        properties:
                          clusterName:
                            description: ClusterName identifies the affected user cluster when the blocker is cluster-specific.
                            type: string
                          count:
                            description: Count is the number of affected objects when the blocker represents an aggregate.
                            format: int32
                            minimum: 0
                            type: integer
                          message:
                            description: Message contains human-readable details about the blocker.
                            type: string
                          seedName:
                            description: SeedName identifies the affected Seed when the blocker is Seed-specific.
                            type: string
                          type:
                            description: Type identifies the class of blocker.
                            enum:
                              - LegacyMachines
                              - InvalidFootprints
                              - UnsupportedFootprintSchema
                              - IncompatibleControllerVersion
                              - StaleHeartbeat
                              - NewCluster
                              - UnreachableSeed
                              - MissingReport
                              - RevisionMismatch
                              - QuotaDigestMismatch
                            type: string
                        required:
                          - type
                        type: object
                      type: array
                    legacyMachinesWithoutFootprint:
                      description: |-
                        LegacyMachinesWithoutFootprint is the project-wide number of Machines that predate
                        trusted footprint capture.
                      format: int32
                      minimum: 0
                      type: integer
                    machinesWithInvalidFootprint:
                      description: |-
                        MachinesWithInvalidFootprint is the project-wide number of Machines whose footprint
                        cannot be accounted.
                      format: int32
                      minimum: 0
                      type: integer
                    observedAccountingRevision:
                      description: |-
                        ObservedAccountingRevision is the authoritative master-issued current accounting
                        revision. Despite the Observed prefix, child reporters must attest to this exact value.
                      type: string
                    observedAt:
                      description: ObservedAt is the oldest heartbeat among the Seed attestations included in this state.
                      format: date-time
                      type: string
                    observedQuotaDigest:
                      description: |-
                        ObservedQuotaDigest is the authoritative canonical digest of the current accelerator
                        quota. Despite the Observed prefix, child reporters must attest to this exact value.
                      type: string
                    ready:
                      description: |-
                        Ready is true when every configured Seed has provided a compatible, fresh attestation
                        for ObservedAccountingRevision and ObservedQuotaDigest and no blocker remains.
                      type: boolean
                  required:
                    - activationPhase
                    - legacyMachinesWithoutFootprint
                    - machinesWithInvalidFootprint
                    - observedAccountingRevision
                    - observedQuotaDigest
                    - ready
                  type: object
                globalUsage:
                  description: GlobalUsage is holds the current usage of resources for all seeds.
                  properties:
                    accelerators:
                      description: |-
                        Accelerators holds provider-specific accelerator limits. An absent or empty list means
                        that no accelerator limits are configured. A missing provider or provider/resource pair
                        is unconstrained; this field is not an allowlist. The list is atomic so a future provider
                        scope can participate in entry identity without redefining provider as the sole map key.
                      items:
                        description: AcceleratorQuota holds accelerator limits for one KKP infrastructure provider.
                        properties:
                          provider:
                            description: |-
                              Provider is the KKP infrastructure provider identifier, not the accelerator vendor.
                              The alpha API supports only kubevirt.
                            enum:
                              - kubevirt
                            type: string
                          resources:
                            additionalProperties:
                              anyOf:
                                - type: integer
                                - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Resources contains provider-native accelerator resource names and their limits.
                              KubeVirt resource names are exact deviceName values. A missing resource name is
                              unconstrained, zero denies that resource, and a positive whole number sets its limit.
                            type: object
                        required:
                          - provider
                          - resources
                        type: object
                        x-kubernetes-validations:
                          - message: accelerator quota resources must not be empty
                            rule: self.resources.size() > 0
                      maxItems: 1
                      type: array
                      x-kubernetes-list-type: atomic
                    cpu:
                      anyOf:
                        - type: integer
                        - type: string
                      description: CPU holds the quantity of CPU. For the format, please check k8s.io/apimachinery/pkg/api/resource.Quantity.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    memory:
                      anyOf:
                        - type: integer
                        - type: string
                      description: Memory represents the quantity of RAM size. For the format, please check k8s.io/apimachinery/pkg/api/resource.Quantity.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    storage:
                      anyOf:
                        - type: integer
                        - type: string
                      description: Storage represents the disk size. For the format, please check k8s.io/apimachinery/pkg/api/resource.Quantity.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                  type: object
                localAcceleratorAccounting:
                  description: |-
                    LocalAcceleratorAccounting contains the Seed-owned accelerator accounting
                    attestation for this ResourceQuota. It is not synchronized from the master.
                  properties:
                    blockers:
                      description: Blockers contains actionable reasons why this Seed-local attestation is not ready.
                      items:
                        description: |-
                          AcceleratorAccountingBlocker describes an actionable reason why accelerator accounting
                          is not ready. SeedName and ClusterName identify the affected scope when applicable.
                        properties:
                          clusterName:
                            description: ClusterName identifies the affected user cluster when the blocker is cluster-specific.
                            type: string
                          count:
                            description: Count is the number of affected objects when the blocker represents an aggregate.
                            format: int32
                            minimum: 0
                            type: integer
                          message:
                            description: Message contains human-readable details about the blocker.
                            type: string
                          seedName:
                            description: SeedName identifies the affected Seed when the blocker is Seed-specific.
                            type: string
                          type:
                            description: Type identifies the class of blocker.
                            enum:
                              - LegacyMachines
                              - InvalidFootprints
                              - UnsupportedFootprintSchema
                              - IncompatibleControllerVersion
                              - StaleHeartbeat
                              - NewCluster
                              - UnreachableSeed
                              - MissingReport
                              - RevisionMismatch
                              - QuotaDigestMismatch
                            type: string
                        required:
                          - type
                        type: object
                      type: array
                    legacyMachinesWithoutFootprint:
                      description: LegacyMachinesWithoutFootprint is the number of Machines that predate trusted footprint capture.
                      format: int32
                      minimum: 0
                      type: integer
                    machinesWithInvalidFootprint:
                      description: MachinesWithInvalidFootprint is the number of Machines whose footprint cannot be accounted.
                      format: int32
                      minimum: 0
                      type: integer
                    observedAccountingRevision:
                      description: ObservedAccountingRevision is the master-issued accounting revision this Seed observed.
                      type: string
                    observedAt:
                      description: ObservedAt is the oldest heartbeat among the cluster attestations included in this report.
                      format: date-time
                      type: string
                    observedQuotaDigest:
                      description: ObservedQuotaDigest is the canonical accelerator quota digest this Seed observed.
                      type: string
                    ready:
                      description: |-
                        Ready is true when every relevant cluster has provided a compatible, fresh attestation
                        for ObservedAccountingRevision and ObservedQuotaDigest and no blocker remains.
                      type: boolean
                  required:
                    - legacyMachinesWithoutFootprint
                    - machinesWithInvalidFootprint
                    - observedAccountingRevision
                    - observedQuotaDigest
                    - ready
                  type: object
                localUsage:
                  description: LocalUsage is holds the current usage of resources for the local seed.
                  properties:
                    accelerators:
                      description: |-
                        Accelerators holds provider-specific accelerator limits. An absent or empty list means
                        that no accelerator limits are configured. A missing provider or provider/resource pair
                        is unconstrained; this field is not an allowlist. The list is atomic so a future provider
                        scope can participate in entry identity without redefining provider as the sole map key.
                      items:
                        description: AcceleratorQuota holds accelerator limits for one KKP infrastructure provider.
                        properties:
                          provider:
                            description: |-
                              Provider is the KKP infrastructure provider identifier, not the accelerator vendor.
                              The alpha API supports only kubevirt.
                            enum:
                              - kubevirt
                            type: string
                          resources:
                            additionalProperties:
                              anyOf:
                                - type: integer
                                - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Resources contains provider-native accelerator resource names and their limits.
                              KubeVirt resource names are exact deviceName values. A missing resource name is
                              unconstrained, zero denies that resource, and a positive whole number sets its limit.
                            type: object
                        required:
                          - provider
                          - resources
                        type: object
                        x-kubernetes-validations:
                          - message: accelerator quota resources must not be empty
                            rule: self.resources.size() > 0
                      maxItems: 1
                      type: array
                      x-kubernetes-list-type: atomic
                    cpu:
                      anyOf:
                        - type: integer
                        - type: string
                      description: CPU holds the quantity of CPU. For the format, please check k8s.io/apimachinery/pkg/api/resource.Quantity.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    memory:
                      anyOf:
                        - type: integer
                        - type: string
                      description: Memory represents the quantity of RAM size. For the format, please check k8s.io/apimachinery/pkg/api/resource.Quantity.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    storage:
                      anyOf:
                        - type: integer
                        - type: string
                      description: Storage represents the disk size. For the format, please check k8s.io/apimachinery/pkg/api/resource.Quantity.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                  type: object
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
