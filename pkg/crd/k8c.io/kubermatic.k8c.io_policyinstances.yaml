---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
    kubermatic.k8c.io/location: master,seed
  name: policyinstances.kubermatic.k8c.io
spec:
  group: kubermatic.k8c.io
  names:
    kind: PolicyInstance
    listKind: PolicyInstanceList
    plural: policyinstances
    singular: policyinstance
  scope: Cluster
  versions:
    - additionalPrinterColumns:
        - description: Whether the policy is applied (only relevant if not enforced)
          jsonPath: .spec.enabled
          name: Enabled
          type: boolean
        - description: cluster or namespace
          jsonPath: .spec.scope
          name: Scope
          type: string
      name: v1
      schema:
        openAPIV3Schema:
          description: |-
            PolicyInstance binds a PolicyTemplate to specific clusters/projects and
            optionally enables or disables it (if the template is not enforced).
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: PolicyInstanceSpec describes how and where to apply the referenced PolicyTemplate.
              properties:
                enabled:
                  description: |-
                    Enabled determines whether we apply the policy

                    If the referenced template is enforced, "enabled" is ignored (or disallowed)
                    If the referenced template is not enforced, "enabled" determines whether we apply the policy
                  type: boolean
                policyTemplateRef:
                  description: PolicyTemplateRef references the PolicyTemplate by name
                  properties:
                    policyTemplateName:
                      description: PolicyTemplateName is the name of the policy template.
                      type: string
                  required:
                    - policyTemplateName
                  type: object
                scope:
                  default: cluster
                  description: |-
                    Scope specifies the Kyverno Policy to be created.

                    The scope can be "cluster" or "namespaced", by default it is "cluster"
                  type: string
                target:
                  description: Target specifies which clusters/projects to apply the policy to
                  properties:
                    allClusters:
                      description: AllClusters indicates that the policy applies to all clusters
                      type: boolean
                    allProjects:
                      description: AllProjects indicates that the policy applies to all projects
                      type: boolean
                    clusterSelector:
                      description: ClusterSelector restricts the policy to clusters matching these labels
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                              - key
                              - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    projectSelector:
                      description: ProjectSelector restricts the policy to projects matching these labels
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                              - key
                              - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                  type: object
              required:
                - policyTemplateRef
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
