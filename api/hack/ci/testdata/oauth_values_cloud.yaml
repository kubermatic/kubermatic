# This file exists only temporarily until we moved the credentials to Vault and fetch
# them on demand. This files is not used for actually installing a Helm chart, but
# only for configuring an OIDC client.

dex:
  ingress:
    host: cloud.kubermatic.io
    path: /dex
  clients:
  - id: kubermatic
    secret: ZXhhbXBsZS1hcHAtc2VjcmV0
    RedirectURIs:
    - http://localhost:8000
