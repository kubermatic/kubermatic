apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: oauth-proxy
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: oauth-proxy
spec:
  ports:
    - port: 4180
      targetPort: 4180
  selector:
    app.kubernetes.io/name: oauth-proxy
    app.kubernetes.io/instance: {{ .Release.Name }}
