alertmanager:
  version: v0.16.0
  host: ""
  config:
    global:
      slack_api_url: https://
    route:
      receiver: 'default'
      repeat_interval: 1h
    receivers:
    - name: 'default'
      slack_configs:
      - channel: '#alerting'
        send_resolved: true
