{{- if .Values.alertmanager.auth_settings.basic.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-kubermatic-auth
type: Opaque
data:
  auth: {{ .Values.alertmanager.auth }}
{{- end -}}
