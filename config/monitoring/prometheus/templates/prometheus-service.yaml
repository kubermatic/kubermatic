apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus-kubermatic
  name: prometheus-kubermatic
spec:
  type: ClusterIP
  ports:
  - name: web
    protocol: TCP
    port: 9090
    targetPort: 9090
  selector:
    app: prometheus
    prometheus: kubermatic
