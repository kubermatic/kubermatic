apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: prometheus-kubermatic
  labels:
    team: kubermatic
spec:
  selector:
    matchLabels:
      app: prometheus-kubermatic
  namespaceSelector:
    matchNames:
    - monitoring
  endpoints:
  - port: web
    interval: 30s
