apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: apiserver
  namespace: monitoring
  labels:
    team: kubermatic
spec:
  selector:
    matchLabels:
      role: apiserver
  namespaceSelector:
    any: true
  endpoints:
  - port: insecure
