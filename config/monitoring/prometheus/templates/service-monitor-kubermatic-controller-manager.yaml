apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kubermatic-controller-manager
  labels:
    team: kubermatic
spec:
  jobLabel: prom-job
  selector:
    matchLabels:
      app: controller-manager
  namespaceSelector:
    matchNames:
    - kubermatic
  endpoints:
  - port: metrics
    interval: 30s
