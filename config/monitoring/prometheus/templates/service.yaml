apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus-kubermatic
  name: prometheus-kubermatic
spec:
  ports:
  - name: web
    port: 9090
    targetPort: web
  selector:
    app: prometheus-kubermatic
