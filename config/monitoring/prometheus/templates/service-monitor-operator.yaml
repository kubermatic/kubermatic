apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: prometheus-operator
  labels:
    team: kubermatic
spec:
  endpoints:
  - port: http
  selector:
    matchLabels:
      app: prometheus-operator
