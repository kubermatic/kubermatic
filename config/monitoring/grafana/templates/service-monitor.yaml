apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: grafana
  labels:
    team: kubermatic
spec:
  selector:
    matchLabels:
      app: grafana
  namespaceSelector:
    matchNames:
    - monitoring
  endpoints:
  - port: grafana
    interval: 30s
