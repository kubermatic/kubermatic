apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus-testing
  name: prometheus-testing
  namespace: monitoring-statefulset
spec:
  ports:
  - name: web
    port: 9090
    targetPort: web
  selector:
    app: prometheus-testing
