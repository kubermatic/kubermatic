apiVersion: v1
kind: Service
metadata:
  name: prometheus-operator
  labels:
    app: prometheus-operator
spec:
  type: ClusterIP
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: http
  selector:
    app: prometheus-operator
