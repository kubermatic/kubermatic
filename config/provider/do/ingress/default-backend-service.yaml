apiVersion: v1
kind: Service
metadata:
  namespace: ingress
  name: default-http-backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    role: default-http-backend
