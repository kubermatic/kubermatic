kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1beta1
metadata:
  name: k8sniff
rules:
- apiGroups: ["extensions"]
  resources: ["ingresses"]
  verbs: ["watch", "list"]
- apiGroups: [""]
  resources: ["services"]
  verbs: ["watch", "list"]
