apiVersion: v1
kind: Service
metadata:
  name: etcd-client
  namespace: cluster-qx6ll4bhvc
spec:
  ports:
  - port: 2379
    name: etcd-client
    protocol: TCP
  selector:
    app: etcd
    cluster: qx6ll4bhvc
