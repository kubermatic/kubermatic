apiVersion: v1
kind: Service
metadata:
  name: etcd
spec:
  type: ClusterIP
  ports:
    - name: clients
      port: 2379
      protocol: TCP
    - name: peer
      port: 2380
      protocol: TCP
  selector:
    role: etcd
