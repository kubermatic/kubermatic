apiVersion: v1
kind: Service
metadata:
  labels:
    name: scheduler
    prometheus: {{ .Cluster.Name }}
  name: scheduler
spec:
  clusterIP: None
  ports:
  - name: http-metrics
    port: 8085
  selector:
    role: scheduler
