apiVersion: v1
kind: Service
metadata:
  name: controller-manager
  labels:
    name: controller-manager
spec:
  clusterIP: None
  ports:
    - name: http-metrics
      port: 10252
      protocol: TCP
  selector:
    role: controller-manager
