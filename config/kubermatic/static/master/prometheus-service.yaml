apiVersion: v1
kind: Service
metadata:
  labels:
    prometheus: {{ .Cluster.Name }}
  name: prometheus
spec:
  type: ClusterIP
  ports:
  - name: web
    port: 9090
    protocol: TCP
    targetPort: web
  selector:
    prometheus: {{ .Cluster.Name }}
