apiVersion: v1
kind: Service
metadata:
  name: etcd
  namespace: cluster-qx6ll4bhvc
  annotations:
    service.alpha.kubernetes.io/tolerate-unready-endpoints: "true"
spec:
  ports:
  - port: 2379
    name: etcd-client
    protocol: TCP
  - port: 2380
    name: etcd-server
    protocol: TCP
  clusterIP: None
  selector:
    app: etcd
    cluster: qx6ll4bhvc
