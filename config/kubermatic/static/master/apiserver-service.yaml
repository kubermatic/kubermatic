apiVersion: v1
kind: Service
metadata:
  name: apiserver
spec:
  type: ClusterIP
  ports:
    - name: secure
      port: {{ .Cluster.Address.ExternalPort }}
      protocol: TCP
    - name: insecure
      port: 8080
      protocol: TCP
  selector:
    role: apiserver
