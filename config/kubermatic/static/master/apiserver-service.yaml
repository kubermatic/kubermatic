apiVersion: v1
kind: Service
metadata:
  name: apiserver
  labels:
    name: apiserver
spec:
  type: ClusterIP
  ports:
    - name: insecure
      port: 8080
      protocol: TCP
  selector:
    role: apiserver
