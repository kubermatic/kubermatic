apiVersion: rbac.authorization.k8s.io/v1beta1
kind: RoleBinding
metadata:
  name: prometheus-{{ .ClusterName }}
  namespace: cluster-{{ .ClusterName }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: prometheus-{{ .ClusterName }}
subjects:
- kind: ServiceAccount
  name: prometheus-{{ .ClusterName }}
  namespace: cluster-{{ .ClusterName }}
