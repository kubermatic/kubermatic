apiVersion: v1
kind: Service
metadata:
  name: kube-state-metrics
  labels:
    name: kube-state-metrics
spec:
  clusterIP: None
  ports:
  - name: http-metrics
    port: 8080
    targetPort: http-metrics
    protocol: TCP
  selector:
    name: kube-state-metrics
