apiVersion: v1
kind: Service
metadata:
  namespace: kubermatic
  name: kubermatic-api
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    role: kubermatic-api
