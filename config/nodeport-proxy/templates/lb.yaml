apiVersion: v1
kind: Service
metadata:
  name: nodeport-lb
  annotations:
    "helm.sh/resource-policy": keep
spec:
  selector:
    app: nodeport-proxy
  ports:
  - name: keepalive
    port: 1337
    protocol: TCP
  type: LoadBalancer
