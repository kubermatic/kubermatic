apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: dex
  namespace: oauth
  annotations:
    kubernetes.io/tls-acme: "true"
    kubernetes.io/ingress.class: "nginx"
spec:
  tls:
  - hosts:
    - {{ .Values.KubermaticURL | trim }}
    secretName: kubermatic-dashboard-tls
  backend:
    serviceName: dex
    servicePort: 5556
  rules:
  - host: {{ .Values.KubermaticURL | trim }}
    http:
      paths:
      - path: /dex
        backend:
          serviceName: dex
          servicePort: 5556
