apiVersion: v1
kind: Service
metadata:
  namespace: kubermatic
  name: kubermatic-bare-metal-provider
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    role: kubermatic-bare-metal-provider
