kind: ConfigMap
apiVersion: v1
metadata:
  name: nginx-configuration
data:
  {{- range $key, $val := .Values.nginx.config }}
  {{ $key }}: {{ $val }}
  {{- end }}

---
kind: ConfigMap
apiVersion: v1
metadata:
  name: tcp-services

---
kind: ConfigMap
apiVersion: v1
metadata:
  name: udp-services
