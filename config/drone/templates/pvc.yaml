kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: drone-data
  annotations:
    volume.beta.kubernetes.io/storage-class: generic
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
