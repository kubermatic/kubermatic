apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards-mla-kubernetes-overview
  namespace: mla
data:
  kubernetes-overview.json: |-
    {{- (.Files.Get "files/kubernetes-overview.json")|nindent 4 -}}
