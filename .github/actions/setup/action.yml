name: setup
description: "shared setup for Go projects"
runs:
  using: composite
  steps:
    - uses: actions/setup-go@v7
      with:
        go-version: 1.26
        check-latest: true
        cache: true
