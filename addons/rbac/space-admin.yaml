apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    component: userClusterRole
  name: space-admin
  namespace: kube-system
rules:
  - apiGroups:
      - "*"
    resources:
      - "*"
    verbs:
      - "*"
