kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: etcd-pvc
  annotations:
    volume.beta.kubernetes.io/storage-class: ssd-storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
